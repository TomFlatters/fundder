class Reward {
  // add properties to the user model here
  final String title;
  final String task;
  final String reward;
  final String function;
  final String rewardId;

  // construct them into the object here
  Reward({this.title, this.task, this.reward, this.function, this.rewardId});
}
